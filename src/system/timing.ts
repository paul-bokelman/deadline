// Named timing constants (ms). Centralized so behavior tweaks happen in one
// place and callers can reason about them by name rather than by magic
// number.

export const REBOOT_AFTER_CALL_MS = 100;
export const REBOOT_RAM_CRASH_DELAY_MS = 3_000;

export const BAILOUT_DEBOUNCE_MS = 250;
export const NETVOICE_QUEUE_DEBOUNCE_MS = 500;

export const BLACKJACK_DEAL_INTERVAL_MS = 350;
export const BLACKJACK_REVEAL_DELAY_MS = 500;
export const BLACKJACK_DEALER_HIT_MS = 600;
export const BLACKJACK_RESET_MS = 900;

export const WINRAR_INSTALLER_INITIAL_PROGRESS_MS = 300;

export const NETVOICE_RING_DELAY_MS = 40;
