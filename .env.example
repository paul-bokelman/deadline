# Public URL of the deadline-api Cloudflare Worker.
# Leave empty to disable network leaderboard features (the game will still run,
# leaderboard will appear empty, and submissions will fall back to local-only).
VITE_API_BASE_URL=https://deadline-api.pabs.workers.dev
